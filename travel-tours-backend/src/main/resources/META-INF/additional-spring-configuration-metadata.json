{
  "properties": [
    {
      "name": "app.cors.allowed-origins",
      "type": "java.lang.String",
      "description": "Comma-separated list of origins allowed to call the API (CORS). Used by JwtWebSecurityConfig. Defaults to http://localhost:3000 for local development; overridden via CORS_ALLOWED_ORIGINS env var in docker-compose and Kubernetes.",
      "defaultValue": "http://localhost:3000"
    }
  ]
}