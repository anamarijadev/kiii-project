apiVersion: v1
kind: Service
metadata:
  name: travel-tours-frontend
  namespace: travel-tours
spec:
  type: ClusterIP
  selector:
    app: travel-tours-frontend
  ports:
    - port: 80
      targetPort: 80
      name: http